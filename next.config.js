/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['*', 'scontent.flhe3-1.fna.fbcdn.net', 'images.unsplash.com', 'naturals.pk', 'cdn.shopify.com']
    }
  };
  
  module.exports = nextConfig;